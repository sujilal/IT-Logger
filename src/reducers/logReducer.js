const initialState = {
  logs: null,
  current: null,
  loading: false,
  cerror: null
};
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
